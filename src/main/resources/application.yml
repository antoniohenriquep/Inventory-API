spring:
  application:
    name: inventory
  datasource:
    url: jdbc:mysql://localhost:3306/inventory
    username: root
    password: *****
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

logging:
  level:
    org:
      hibernate:
